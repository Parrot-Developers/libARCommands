<?xml version="1.0" encoding="UTF-8"?>

<!--
   Important notes :
   ALWAYS ADD NEW THINGS AT THE END OF THE PARENT TAG !
   NEVER MODIFY EXISTING class/command/arg !

   Comments ("comm" attributes) are mandatory for all classes/commands/args
   For each comment : 1st line will be used as brief (if applicable) +
   1st line of complete desc. Other lines will be used as complete desc

   How to add a new class :
   <class name="myNewClassName" >           | No spaces ! [A-Za-z0-9_]
     Comment about the class                | Inlcuded in Doxygen doc
     <cmd> ... </cmd>                       | List of commands
     <cmd> ... </cmd>
   </class>

   How to add a new command (without args) to a class :
   <cmd name="myNewCommandName" >           | No spaces ! [A-Za-z0-9_]
     Comment about the command              | Included in Doxygen doc
   </cmd>

   How to add a new command (with args) to a class :
   <cmd name="myNewCommandWithArgs" >       | No spaces ! [A-Za-z0-9_]
     Comment about the command              | Included in Doxygen doc
     <arg name="argName" type="argType" >   | Repeat for each arg
       Comment about the arg                | argName must be a valid C variable name
     </arg>                                 | argType must be a valid type (see below)
     ...                                    | Comment included in Doxygen doc
   </cmd>

   List of valid types (associated C type) :
   -  i8 /  u8 : signed/unsigned  8 bit integer ([u]int8_t)
   - i16 / u16 : signed/unsigned 16 bit integer ([u]int16_t)
   - i32 / u32 : signed/unsigned 32 bit integer ([u]int32_t)
   - i64 / u64 : signed/unsigned 64 bit integer ([u]int64_t)
   -     float : IEEE-754 single precision floating point number (float)
   -    double : IEEE-754 double precision floating point number (double)
   -    string : NULL-terminated UTF-8 String. (char *)
-->


<project id="3">
  All commands specific to the Jumping Sumo.

  <class name="pilotingAction" id="0">
    All commands related to piloting the JumpingSumo

    <cmd name="setSpeed">
      Ask the JS speed and turn ratio.

      <arg name="speed" type="i8">
        Speed value [-100:100].
      </arg>

      <arg name="turnRatio" type="i8">
		  Turn ratio value. [-100:100]
      </arg>

      <arg name="touchScreenFlag" type="u8">
        Boolean for "touch screen".
      </arg>

    </cmd>

    <cmd name="requestFullStateRefresh">
    Request the JS to send all the possible piloting events in order to retrieve the full state of the JS.
    </cmd>

    <cmd name="setPostureJumper">
        Request the JS to switch to JUMPER posture.
    </cmd>

    <cmd name="setPostureKicker">
        Request the JS to switch to KICKER posture.
    </cmd>

    <cmd name="setPostureStanding">
        Request the JS to switch to STANDING posture.
    </cmd>
    
    <cmd name="jumpStop">
		Ask the JS to STOP jump action, emergency jump stop, stop jump motor and stay there.
    </cmd>

    <cmd name="jumpCancel">
		Ask the JS to CANCEL jump action and come back to previous state (if possible).
    </cmd>
    
    <cmd name="jumpLoad">
		Ask the JS to LOAD a jump.
    </cmd>

    <cmd name="jumpLong">
		Ask the JS to do a LONG jump.
    </cmd>

    <cmd name="jumpHigh">
		Ask the JS to do a HIGH jump.
    </cmd>
    
  </class>
  
  <class name="pilotingEvent" id="1">
  Occasional feedback on piloting data.

    <cmd name="postureChangedToJumper">
		Sent by the Jumping Sumo when its posture changes to JUMPER.
    </cmd>

    <cmd name="postureChangedToKicker">
		Sent by the Jumping Sumo when its posture changes to KICKER.
    </cmd>

    <cmd name="postureChangedToStanding">
		Sent by the Jumping Sumo when its posture changes to SEGWAY.
	</cmd>

    <cmd name="postureChangedToStuck">
		Sent by the Jumping Sumo when its posture changes to STUCK.
	</cmd>

	<cmd name="jumpLoadStateChangedToUnknown">
		Sent by the Jumping Sumo when the jump load state changes to UNKNOWN.
    </cmd>

    <cmd name="jumpLoadStateChangedToUnloaded">
		Sent by the Jumping Sumo when the jump load state changes to UNLOADED.
    </cmd>

    <cmd name="jumpLoadStateChangedToLoaded">
		Sent by the Jumping Sumo when the jump load state changes to LOADED.
    </cmd>

    <cmd name="jumpLoadStateChangedToBusy">
		Sent by the Jumping Sumo when the jump load state changes to BUSY.
    </cmd>
    
  </class>
  
</project>
